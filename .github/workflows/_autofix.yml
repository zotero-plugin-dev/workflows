name: autofix.ci

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main

jobs:
  autofix:
    uses: zotero-plugin-dev/workflows/.github/workflows/autofix.yml@main
    permissions:
      contents: read
    with:
      command: "pnpm lint"
